import {ReferentService} from './service/referent-service';

/**
 * visible for testing.
 * @abstract
 */
class AmpAnnotator extends AMP.BaseElement {
  constructor(element) {
    super(element);
  }
}
